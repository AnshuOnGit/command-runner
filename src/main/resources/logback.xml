<?xml version="1.0" encoding="UTF-8"?>
<configuration>

<property name="HOME_LOG" value="master.log"/>

<timestamp key="bySecond" datePattern="yyyyMMdd'T'HHmmss"/>

	<appender name="STDOUT"
		class="ch.qos.logback.core.ConsoleAppender">
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder 
			by default -->
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{5} - %msg%n
			</pattern>
		</encoder>
	</appender>
<!-- 
	<appender name="FILE-ROLLING"
		class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>c:/Users/ankumar/dev/wfosaas/logs/logstimeBasedlogFile.%d{yyyy-MM-dd-HH-mm}.%i.log</fileNamePattern>
            each archived file, size max 10MB
            <maxFileSize>10MB</maxFileSize>
            total size of all archive files, if total size > 20GB, it will delete old archived file
            <totalSizeCap>20GB</totalSizeCap>
            60 days to keep
            <maxHistory>60</maxHistory>
        </rollingPolicy>
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{5} - %msg%n
			</pattern>
		</encoder>
	</appender> -->
	<appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>c:/Users/ankumar/dev/wfosaas/logs/master.log</file>
        <append>false</append>
        <encoder>
            <Pattern>%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</Pattern>
        </encoder>
    </appender>

	<root level="info" additivity="false">
        <appender-ref ref="STDOUT"/>
        <appender-ref ref="FILE"></appender-ref>
    </root>

</configuration>